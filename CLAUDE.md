# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What This Is

**End goal, stated explicitly because it governs prioritization:** enable Claude Code to do *agentic programming* of Desynced behaviors — read, review, and edit a real in-game behavior the way it would work with source code in a normal IDE, then write the result back into the live game. Everything else in this repo (the `.dcs` wire codec, the compiler, the Lua-backed interpreter) is **instrumental infrastructure in service of that goal**, not the goal itself. As of 2026-07-06 the actual game-to-Claude transport for this turned out to already exist for free — Desynced's own copy/paste (`Tool.GetClipboard`/`SetClipboard`, producing the same `.dcs` string this repo's codec decodes) plus a VirtualBox host/guest shared clipboard gives a live, bidirectional read/write channel with zero new game-side code. The remaining open problem is representation: Desynced behaviors are genuine flow graphs (arbitrary-fan-in control edges, arbitrary-fan-out data edges — confirmed against real user behaviors, not just our own generated ones), and forcing that into tree-structured pseudocode (this repo's current Python-like compiler target) is a lossy fit under active reconsideration — see `desynced-toolkit` below and project memory for where that stands.

This repo is tooling, documentation, and design work built *on top of* an extract of the base game data assets for **Desynced** (by The Desynced Team), a factory/automation game with programmable robots — the Lua scripts, textures, sounds, and other assets that define game behavior. There is no build system for the game data itself — it's a read-only reference for analyzing and understanding game mechanics.

**The game data extract lives outside this repo** (moved out on 2026-07-04 to stop ~560MB of vendored, untracked game assets from cluttering a tooling project — see `desynced-toolkit` below for the actual thing being built here). You need your own local copy of it; this repo does not include or fetch it. **Convention:** place it as a sibling directory of this repo, named `desynced-game-data` (i.e. if this repo is at `/path/to/desynced/`, the extract goes at `/path/to/desynced-game-data/`) — everything in this repo that looks for it (tests, tooling) defaults to that location with no configuration. If you put it somewhere else, set the `DESYNCED_GAME_DATA` env var to its path. Every path mentioned in this doc as if it were local (`data/instructions.lua`, `ui/ui.lua`, `main.zip`, etc.) is **relative to that external directory**, not this repo's root. Both Bash and Read can reach it directly via absolute path without any special setup; use `/add-dir <path-to-your-extract>` if a given session needs it added explicitly. `desynced_toolkit.assets.open_asset_source(...)` (this repo's own code) takes that directory (or its `main.zip`) as a plain argument — nothing is hardcoded.

The entry point manifest is `def.json` (in the external directory), which defines all packages and their load order.

## Package Architecture

The game uses a package system. Each package has an `entry` Lua file that sets `package.includes` (a list of files to load in order) and lifecycle callbacks:

- `package:init()` — runs at startup, registers data definitions into global tables
- `package:init_ui()` — UI-only initialization
- `package:setup_scenario(settings)` — called before scenario starts
- `package:validate_data()` — dev-mode data integrity checks
- `package:on_update(ver)` — migration logic for saved games upgrading from old versions

The packages defined in `def.json`:

| Package | Entry | Type | Purpose |
|---|---|---|---|
| `Data` | `data/data.lua` | Data | All game definitions (items, frames, components, etc.) |
| `UI` | `ui/ui.lua` | UI | In-game HUD, menus, input bindings |
| `FrontEnd` | `frontend/frontend.lua` | Scenario | Main menu and lobby |
| `Freeplay` | `scenarios/freeplay.lua` | Scenario | Primary open-world game mode |
| `Versus` | `scenarios/versus.lua` | Scenario | PvP scenario |
| `Resim/Registers1/Registers2/Signals/Nomad/TowerD` | `scenarios/*.lua` | Challenge | Puzzle/challenge scenarios |

## Core Data Definitions (`data/`)

All game definitions are registered into global `data.*` tables. Each definition file documents its own schema at the top as a comment block.

**Key definition tables:**

- `data.frames` — Entity "frames" (the physical body of a unit or building). Built with `Frame:RegisterFrame(id, def)`. Frame definitions include movement speed (non-zero = unit/bot), health, slots, initial components, and lifecycle callbacks (`on_placed`, `on_destroy`, `on_interact`).
- `data.components` — Attachable components that go into frame sockets. Define behavior, production recipes, power consumption, registers, and event callbacks (`on_add`, `on_remove`, `on_update`, `on_trigger`, `on_take_damage`).
- `data.items` — Inventory items with stack sizes and recipes.
- `data.instructions` — The visual programming instruction set. Each instruction has `func(comp, state, cause, ...)` implementing its behavior. Instructions are categorized: Flow, Unit, Global, Math, Move, Component, AutoBase.
- `data.behaviors` — Pre-encoded behavior programs (stored as base62-encoded strings).
- `data.techs` — Technology tree nodes with uplink recipes and unlock lists.
- `data.explorables` — Procedurally placed world events/encounters. Each explorable implements `GetRelevancy(x, y, info)` and `SpawnExplorable(x, y)`.
- `data.values` — Enum-like named values (colors, radar filter types, alien signals).
- `data.visuals` / `data.visualassets` — Visual representations and socket positions.
- `data.fx` — Sound/particle effects.
- `data.biomes`, `data.landfeatures`, `data.cliffs` — World generation parameters.
- `data.puzzles` — Minigame puzzle definitions.

**Recipe helpers** (defined in `data/utilities.lua`):
- `CreateProductionRecipe(ingredients, producers, amount)` — manufactured items/components
- `CreateMiningRecipe(miners)` — mined resources
- `CreateUplinkRecipe(ingredients, ticks)` — research via uplink
- `CreateConstructionRecipe(ingredients, ticks)` — placed buildings

## Faction & Entity System

The game world is divided into **factions**: `player` (human-controlled), `world` (neutral explorables), `bugs`, `alien`, `anomaly`. Trust relationships between factions determine hostility. Faction-level data persists in `faction.extra_data`.

**Entity registers** are the core of the programming system. Frames have a fixed number of registers (FRAMEREG_GOTO, FRAMEREG_STORE, etc.) that hold typed values (entities, items, numbers, signals). The visual behavior editor programs these by constructing instruction sequences.

**Behaviors** are programs attached to components (particularly `c_behavior` and `c_autobase`). The `data/library.lua` handles compiling encoded behavior strings into bytecode (`GetFactionBehaviorAsm`) and caching them.

## Action System

The game uses typed action dispatchers:

- `PlayerAction.*` — actions sent by a player, validated server-side
- `EntityAction.*` — actions applied to a specific entity
- `FactionAction.*` — actions applied to a faction
- `MapMsg.*` — broadcast map-wide messages
- `UIMsg.*` — UI-only callbacks (run only on local client)
- `Chat.*` — multiplayer chat message handlers
- `Delay.*` — deferred map-tick callbacks
- `MapRun.*` — simulation-side broadcast

## Explorable System

Explorables are the world's discoverable encounters. The file `data/explorables.lua` contains `Explorable_SpawnAt(x, y, discoverer, filter)` which selects an explorable by weighted `GetRelevancy` scores based on tile data (blightness, elevation, richness), player faction level, and save state. Individual explorable definitions are in `data/explorables/`.

## Technology Tree

Defined in `data/techs.lua` and split across `data/tech/tech_*.lua` by faction (robots, blight, alien, human, virus). Tech categories are in `data.tech_categories`. Each tech has `require_tech`, `unlocks` (array of ids it enables), and an `uplink_recipe`.

## Scenarios

Each scenario (in `scenarios/`) extends the base `Data` package:
- `freeplay.lua` — primary game mode; includes tutorial, events, codex entries, and explorable config for the open world
- `versus.lua` — PvP with fixed maps (map data in `scenarios/map_data/`)
- Challenge scenarios (`registers1`, `registers2`, `signals1`, `nomad1`, `towerdefense`, `resimulator`) — self-contained puzzles with specific win conditions

## UI Architecture (`ui/`)

`ui/ui.lua` is the entry point. Key subsystems:
- `widgets.lua` / `utilities.lua` — base UI helpers
- `Program.lua` — visual behavior/instruction editor
- `BlueprintEditor.lua` / `LinkEditor.lua` — blueprint and register linking UI
- `Library.lua` — saved behavior library management
- `FrameView.lua` / `BuildView.lua` — entity inspection and build menus
- `Tech.lua` / `Codex.lua` — tech tree and in-game encyclopedia

Input bindings use `Input.BindAction(name, event, handler)`. Default bindings are in `InputDefaultActionMappings` at the bottom of `ui/ui.lua`.

## Factions in the World

| Faction ID | Description |
|---|---|
| `player` / `player_N` | Human-controlled player factions |
| `world` | Neutral world entities (resources, explorables) |
| `bugs` | Hostile creatures; hostility controlled by `peaceful` setting (0=none, 1=passive, 2=aggressive, 3=swarm) |
| `alien` | Ancient alien civilization ruins and units |
| `anomaly` | The robot anomaly faction (AI opponent) |

## Asset Layout

(All paths below are under the external game-data directory — see "What This Is".)

- `textures/` — PNG icons organized by category (items, frames, components, effects, codex images)
- `sounds/` — Audio assets
- `skin/` — UI skin assets (buttons, panels, icons)
- `languages/` — Localization files for 12 languages
- `scenarios/maps/` — Heightmap TGA files for versus/challenge maps

## `desynced-toolkit` (`pyproject.toml`, `src/desynced_toolkit/`)

A real Python package (managed with `uv`), begun as a Python-syntax-like compiler/decompiler for behaviors, with runtime semantics backed by `lupa` (an embedded real Lua interpreter) running the **actual, unmodified `data/instructions.lua`** rather than hand-reimplemented-in-Python semantics — the source of most bugs found earlier in this project (see `behavior_format.md`'s revision history). Everything past the wire-bytes layer works with **genuine Lua tables** (1-based, via `lupa`) rather than a Python dict standing in for one — see `dcs_wire.py` below for why that's not just a style preference.

**Setup:** `uv sync` (installs `lupa` + dev deps like `pytest` from `pyproject.toml`/`uv.lock` into `.venv/`, which is gitignored and not checked in). Needs the external game-data extract, conventionally a sibling directory named `desynced-game-data` (see "What This Is" above) — tests skip cleanly if it's missing there and `DESYNCED_GAME_DATA` isn't set to point elsewhere.

**Lua version pin:** every module that touches a `LuaRuntime` imports `lupa.lua54 as lupa`, not bare `import lupa`. `lupa`'s own top-level module resolves to whatever Lua version it was built against by default — in the currently-installed `lupa` release that's **5.5**, not 5.4 — while the real shipped game (confirmed 2026-07-05 by disassembling `Desynced.exe`'s embedded Lua version string) embeds **Lua 5.4.4** exactly. `lupa.lua54` is a separate submodule bundling actual Lua 5.4 with the identical API surface (`LuaRuntime`, `lua_type`, ...), so aliasing the import is the entire fix — no other code changes needed. This matters because `lupa.lua_type()` from one version's submodule silently returns `None` (not an error) on a table produced by a *different* version's `LuaRuntime`, so a partial fix (pinning `LuaRuntime` in one file but leaving a bare `import lupa` for `lua_type` checks elsewhere) would silently break table-detection rather than fail loudly. All 684 tests pass unchanged after the pin, confirming no test in this suite happened to depend on a 5.4-vs-5.5 behavioral difference.

**Tests:** `uv run pytest tests/` (684 tests as of this writing, ~3.5s). Covers: real-Lua arithmetic/`check_number`/coordinate ops including `REG_INFINITE` (`test_lua_runtime.py`), `dcs_wire.py` decode/round-trip on every real `.dcs` file in the repo (`test_dcs_wire.py`), `HexAt`/`HexIndexOf` against closed-form reference math *and* round-tripped through each other, both executed via the real Lua interpreter (`test_hex_expansion.py`), and the compiler including the equality-handling regression (`test_compiler.py`). Run this after any change here before concluding something works — it's what actually backs the claims in this section, not just conversation history.

**Standing convention — reuse the real Lua, both data and logic:** don't reimplement in Python anything the actual game already does. This has two parts, both with real examples in this codebase: (1) *data shape* — build genuine Lua tables (1-based, via the running `lupa.LuaRuntime`) for anything the game itself represents as a Lua table, rather than a Python dict/list rendering (this is why `dcs_wire.py` replaced `dsc_codec.py`, see below); (2) *logic/decisions* — if a real function already exists in the extract that makes some decision (a comparison, a branch, an addressing calculation), call it and use its actual result, don't re-derive the same decision in Python from values you happened to read back (this is why `check_number`'s branch decision is now resolved by calling the real func with real target values, not by comparing numbers in Python — see `interpreter.py` below). Neither is about performance or purity: an extra representation/logic layer standing in for the real thing is where this project's bugs have come from, repeatedly.

- `assets.py` — loads package files from either an extracted directory or directly out of a zip (e.g. `main.zip`) via stdlib `zipfile`, no extraction to disk needed *for the library*. (This is a Python-level abstraction only: Claude's own Read/Grep/Bash tools still can't see inside a zip, so ad hoc extraction remains the move for interactive exploration in conversation.)
- `dcs_wire.py` — the base62 + optional-zlib + custom-MessagePack-variant codec for Desynced Clipboard Strings ("DCS", this project's own coined term — not an in-game or community name; renamed 2026-07-07 from this project's earlier "dsc", which was a misnomer: the wire prefix is just `DS` + a one-character type tag (`'C'` = behavior/code, `'B'` = blueprint, others uninvestigated), never literally "DSC" — see `dcs_wire.py`'s module docstring) — covering `data.behaviors` entries, player clipboard strings like `observer.dcs`. **Replaces the project's original standalone `dsc_codec.py` script** (deleted from the repo root — its logic lives here now), with one real change: `decode_dcs`/`encode_dcs` build/consume genuine Lua tables (via a `lupa.LuaRuntime`) instead of a Python `dict`/`list` rendering. This isn't just tidiness — `Tool.GetClipboard()`/`Tool.SetClipboard(item, type)` (`ui/Library.lua`) are the *real* functions that do this conversion in the actual game, confirmed engine-native (no Lua source for base62/msgpack/clipboard access anywhere in this extract), so a genuine Lua table *is* the most-real representation available — and as of 2026-07-05, "engine-native" is no longer just an inference from absent Lua source: disassembling `Desynced.exe` (UE5 Shipping build) with `radare2`/`r2ghidra` located the actual native `Tool.*` Lua-registration table and traced `GetClipboard`'s inner decode worker structurally against this module — the `"DS"` magic-byte check, the type-char position, and the base-31 length-header decode loop all matched with no discrepancies found (see memory for the full trace; stopped before the innermost checksum sub-function since the wire format was already cross-validated two other ways). The same table also revealed several native `Tool.*` functions this project doesn't use yet and hasn't documented elsewhere: `EncodeEntityStates`, `EncodeTrustLevels`, `GetTableDelta`/`ApplyTableDelta`, `TableToString`/`StringToTable`, `CompareStringNoCase`, `ContainsStringNoCase`, `MatchLocalizedRichText`, `GetDateStr`, `GetTimeDurationStr`, `ParseEntityStates`; the old tool's 0-based Python dict rendering was an artifact of JSON/Python convenience, not something intrinsic to the format, and it caused a real bug (`decode_dcs()`'s native return used `int` keys while hand-built/JSON dicts used `str` keys — every `dsc_codec.py decode <file>` CLI example elsewhere in this project only ever *looked* string-keyed because that was `json.dumps` doing the coercion for display, not the decoder itself). Building straight into Lua's own 1-based array convention removes that whole class of bug along with the representation layer that caused it. Still the same base62/zlib/msgpack-variant bytes on the wire — cross-validated against the retired tool's own output across all real `.dcs` files in this repo (exact structural match both directions) before it was deleted, and against the official [StageGames/DesyncedJavaScriptUtils](https://github.com/StageGames/DesyncedJavaScriptUtils) `dsconvert.js` reference implementation before that (two real bugs found in *their* encoder, documented in `dcs_wire.py`'s module docstring, deliberately not reproduced). It does **not** use a standard MessagePack library (repurposes type bytes `0xc1`/`0xc4`/`0xc5` for non-standard meanings) and has no instruction-metadata layer or disassembler — string ⟷ Lua-table transport only.
- `engine_stub.lua` / `lua_runtime.py` — the `lupa`-backed runtime. Key discovery, documented in both files: `Get`/`Set`/`GetNum` are **not** external engine globals as `behavior_format.md` originally assumed — they're local aliases *within* `instructions.lua` itself for real `InstGet`/`InstSet`/`InstGetNum`/`InstBeginBlock` functions also defined in that file, resolving arguments through a `GetStack`-based unified integer addressing scheme (`state.mem[]` for locals/literals, `comp.owner:GetRegister()` for frame registers). These are reused **verbatim, unmodified** — only the genuinely-external pieces are stubbed: the `Value` type's arithmetic (confirmed engine-side), a `comp`/`comp.owner`/`Tool.NewRegisterObject` stand-in, and `GetCachedBehaviorAsm` (needed for `jump`/`label`). `LupaEngine.decode_dcs`/`encode_dcs` wrap `dcs_wire.py` for the `Tool.GetClipboard`/`SetClipboard`-equivalent conversion (see above for why they're exposed as plainly-named methods rather than bound onto those exact Lua names — the real functions read/write the OS clipboard with no string argument, which isn't what we're simulating).
- `interpreter.py` — runs a behavior given as a genuine Lua table (1-based, the shape `dcs_wire.decode_dcs()` hands back and `compiler` produces directly — no Python dict anywhere in this module) by delegating each leaf instruction to the real Lua (via `lua_runtime.py`) while keeping the branch/block control-flow driver in Python (the same model `behavior_format.md`'s "Block-type instructions" section documents, validated against real in-game logs earlier in this project). Validated: the real `HexAt` sub-behavior (decoded straight from `HexIndexOf_test_1.dcs` via `dcs_wire.py`) run through this interpreter matches the known-good reference for all 91 in-domain `(R, T)` cases, with every arithmetic/branch/coordinate instruction executing genuine game Lua. `check_number`'s branch decision is now also genuinely delegated (resolve If Larger/If Smaller to raw asm targets ourselves, pass them straight through as the real func's own args, read `state.counter` back) rather than re-derived in Python — an earlier draft only delegated the value reads and re-did the comparison itself, confirmed behaviorally equivalent (including `REG_INFINITE`) but not literally "real," before being fixed to match `jump`'s already-fully-delegated approach. `sequence`/`for_number`'s block-stack driving is simulated in Python rather than reusing the real `InstBeginBlock` (confirmed present and callable in `data/instructions.lua`) — genuinely reusing it is still deferred, since it requires also replicating (or reusing) the real per-tick dispatcher's dead-end/`state.blocks` handling in `data/library.lua`, not yet examined. `for_number` and `last` (Break) are now both implemented on top of that same Python-simulated block stack (previously neither was handled at all — any compiled program using either op would hit the `unhandled op` fallback), following the convention above: the per-iteration advance/termination decision (including the documented `Step` auto-direction quirk and `REG_INFINITE` wraparound) is genuinely delegated to the real `data.instructions.for_number.next`/`.last`, with only the small, already-pinned-down initial-offset formula ported by hand (calling the real `BeginBlock` for this would require adopting the real dispatcher's own `state.counter` pre-seeding convention, a bigger change bundled with the `InstBeginBlock` reuse above). Fixing this also surfaced and fixed a real pre-existing gap: falling off the true end of the instruction array without an explicit `next: false` never consulted the block stack at all, silently ending the run instead of popping to an enclosing loop/sequence — the main dispatch loop now checks bounds itself and calls the same dead-end path every time, not just on an explicit `false`.
- `compiler/ast_compiler.py` — compiler using Python's own `ast` module to parse a subset of Python syntax directly into a genuine Lua table (takes a `lupa.LuaRuntime` to build it). Grew substantially from a first-cut (assignment, `+ - * //`, a single flat `if a > b / a < b: ... else: ...`) while compiling `hex_expansion_math.md`'s revised `HexIndexOf` (see that section below): arbitrarily nested arithmetic and unary negation now hoist sub-expressions into fresh temporaries instead of only accepting one flat `a op b`; `if`/`elif`/`else` also handles `==`/`!=`/`>=`/`<=` and a top-level `a and b` chain (still no `or`, no nested boolean expressions); `if cond: continue` (no `else`) inside a loop compiles to an explicit `false`-wired branch rather than real instructions — the same "not a match, try the next one" idiom this project's hand-authored behaviors already use, just now recognized as a distinct pattern from a normal `if`/`else` (its "which outcome dead-ends" wiring is the *complement* of the normal case's, a real bug caught and fixed during this work: the first draft wired the wrong outcome to `false` for every op except the `if`/`else` cases it happened to share code with); `for x in range(a, b):` / `break` compile to `for_number`/`last`; `jump(value)`/`label(value)` are recognized as bare statement-calls; `abs(x)`/`max(a, b, ...)` macro-expand into the check_number-guarded idioms `hex_expansion_math.md` documents (no native instruction for either) by synthesizing an equivalent `if`/assignment AST and recursing, rather than hand-emitting new instruction-building code; `a, b = separate_coordinate(x)` and `x = combine_coordinate(a, b)` call the real coordinate instructions directly. A real equality-handling bug was found and fixed early on: the `if a > b` form used to route the `a == b` case into the wrong branch (fixed by always compiling the Python `else` as the physically-next fallthrough, regardless of which comparison direction is being tested — see the instruction's own comment for why). Confirmed to round-trip through `dcs_wire.py`'s encode/decode cycle. Still no declared sub-behavior `parameters`/`pnames` (a compiled program's inputs/outputs are just plain named locals, seeded/read directly through `Interpreter.mem` in tests) — see `tests/test_hexindexof_compiled.py` for the current ceiling of what this compiles.

- `bsf/` — a real, working, bidirectional package for `behavior_source_format.md`'s BSF grammar (built 2026-07-08, per that doc's own "Status" section for the full detail): `decompile.py` (real Lua table → `BsfBehavior` graph IR), `compile.py` (IR → real Lua table, the "assembler" pass resolving node ids to wire positions fresh on every call), `render_text.py`/`parse_text.py` (IR ⟷ BSF text), `render_mermaid.py` (IR → Mermaid flowchart), `values.py`/`ir.py` (the value/node/behavior dataclasses), `argcache.py` (the shared live-`data.instructions[op].args`-backed arg-position/exec-branch resolution logic, relocated out of `scripts/render_examples.py`, which is now a thin CLI wrapper over this package). Validated against all 9 real `.dcs` fixtures in `tests/data/` two ways (table-level round-trip, and the same with the BSF text layer in between) plus a genuine end-to-end exercise (decode → edit-as-text → recompile → re-encode → re-decode, `tests/test_bsf_end_to_end.py`). Six real gaps in the base grammar were found and closed during this build, not anticipated by the spec text — parameter direction (a display-only `NAME*`, computed fresh from real usage including transitively through `call`, never trusted from the wire's `parameters[i]` bit, which is only a UI-drawing hint — user-confirmed, not guessed), `make_asm` hidden literal fields (`sub`/`c`/`txt`/`cmt` as ordinary `name=value` pairs), `keepvars`/`keeparrays` (two independent behavior-level persistence flags — "Variables" vs. "Memory Arrays" in the real editor's own options popup, `ui/Program.lua`; `keeparrays` is 3-state, not boolean) — found 2026-07-09 when the user hand-constructed two real behaviors, identical instructions/params/vars, differing only in `keepvars`, specifically to test whether this pipeline could see the difference; it initially couldn't (`render_text.py`/`parse_text.py` silently dropped `keepvars` despite the table layer already handling it correctly, and `keeparrays` turned out to be unhandled at every layer, undocumented anywhere in this project until found chasing that same bug down) — see the doc's own "Six real gaps closed during implementation" section, and `behavior_format.md`'s "Top-level envelope" for the further-traced finding that both flags are consulted only for the outermost behavior assigned to a component, never for a `call`ed sub-behavior (a subroutine's own locals are always freshly allocated per-call and discarded on return, regardless of its own `keepvars`) — a bare bool at a non-exec value arg slot (compiler-equivalent to that slot being absent, not a distinct value, confirmed against `GetFactionBehaviorAsm`'s own arg-resolution loop treating `nil` and a bare bool identically), and the top-level `desc` field (the text layer already fully supported it; only `decompile.py`'s read was ever missing) — both found 2026-07-09 via a second user-constructed real-world test comparing a behavior using the deprecated `for_signal` instruction against the same behavior hand-rebuilt in the current editor (`tests/data/deprecated_haul_to_signal.dcs`), which also surfaced a real, previously-undocumented engine fact independent of any tooling bug: the original has no wiring at all on `domove`/`dodrop`'s "Path Blocked" exec pin, but user-confirmed this isn't an authorial omission — that pin didn't exist on either instruction when the original behavior was authored, added to the schema only in a later game update, and the current editor visibly can't tell "deliberately unwired" apart from "pin didn't exist yet" for old data (see the gotchas list below). The compiled effect is the same either way (a blocked path silently falls through and is treated like a successful move) — the rewrite, built fresh, wires "Path Blocked" to an explicit dead end instead — but the causal story ("the original author chose to ignore this") doesn't hold up. The old "STOP" branch label was renamed to `POP` mid-build (user correction): it doesn't halt, it pops the current context frame (innermost active loop or `call`), and an empty stack auto-restarts from Program Start as the unremarkable consequence of popping with nothing left, not a separate case — `exit` remains the only genuine, always-explicit halt. `render_mermaid.py` draws every declared exec pin as its own labeled edge (wired or not, sourced from live `data.instructions[op].args`), with an unwired pin routed to its own small local marker rather than one shared terminal node (rejected as pure visual noise on a real graph). The top-level `next` pin's own label/existence is now real per-op data too, not a placeholder: `data.instructions[op].exec_arg` (found via user correction, previously unread by this project) names it for real when present (`check_number`'s "If Equal") and its being explicitly `false` means the op has no top-level pin at all (`exit`/`restart`/`last` — matching the real editor, which draws none for them either) — `argcache.next_pin_name(op)` is the single source of truth both render and parse sides use, so `parse_text.py` now needs `ArgCache` too (an earlier draft didn't). **Explicitly not attempted, not just deferred:** any static resolution of what a `POP`'s destination actually is inside a loop's dynamic scope — genuinely undecidable in general (halting-problem-equivalent), see the doc's "Loop-type instructions" section. Migrating `ast_compiler.py`'s role onto this and rewriting `hex_expansion_math.md`'s compiled `HexIndexOf` directly in BSF are separate, later, not-yet-started follow-on work.
- `render_mermaid.py`'s layout was substantially reworked 2026-07-10, both times by building the previous design against a real behavior and looking at the actual result rather than deciding in the abstract. First: `flowchart LR` (matching the real editor's own left-to-right convention, confirmed user fact) was tried, rendered against a real 39-node behavior, and reverted to `flowchart TD` — Mermaid's dagre auto-layout has no equivalent of the real editor's own bounding-box collision avoidance between disconnected chains, so LR sprawled far too wide for a diagram read by scrolling a browser page (vertical scrolling comfortable, horizontal not, unlike the real editor's pan/zoom canvas) — a deliberate mismatch with the source tool, not an oversight. Second, and bigger: `render_mermaid` now returns `list[str]`, one diagram per **component**, found by **forward reachability from Program Start** — not undirected connectivity, which was tried first and came back with an entire real behavior (Mining Leader V3.2) as a single component, because every one of its labeled state-machine sections has its own static `jump` back to the main loop even though the *forward* dispatch into it is an unresolvable dynamic `jump(Label=$State)`; undirected connectivity treated that return edge as enough to weld everything into one piece. User-confirmed correction that unblocked the real fix: an implicit fallthrough ("physically next") is just a compact *encoding* of a real, deliberate edge, never a lesser kind of connection — the actual bug was reachability *direction*, not edge weighting. An edge leaving a component renders as a small local "external reference" marker, never a broken edge or a false merge. `direction` and `connect_resolved_jumps` (whether a resolved `jump→label` edge merges components or always stays a boundary) are real, user-requested parameters, not fixed choices — see `behavior_source_format.md`'s "Visualization" section for the full reasoning and `scripts/render_examples.py` for the updated `<out_prefix>.<i>.mmd`-per-component CLI output.
- `bsf/__main__.py` (added 2026-07-10) makes the `bsf` package directly runnable as `python -m desynced_toolkit.bsf {decompile,compile}`, reading a `.dcs` string or BSF text on stdin and writing the other on stdout (`--input`/`--output` file flags also accepted) — a thin wrapper over `dcs_to_bsf`/`bsf_to_dcs` (the `bsf/__init__.py` one-call helpers). Game-data resolution follows the same convention as `tests/conftest.py`: sibling `../desynced-game-data` dir by default, overridable with `--game-data` or `$DESYNCED_GAME_DATA`. Built specifically to sit in a shell pipeline against the game's own clipboard (this repo's actual game-to-Claude transport, see "What This Is" above), e.g. with `cb` a user-side wrapper script for `xclip -selection clipboard`: `cb -o | python -m desynced_toolkit.bsf decompile > mybehavior.bsf`, hand-edit, then `python -m desynced_toolkit.bsf compile < mybehavior.bsf | cb -i`. Only handles a top-level behavior/program clipboard item (wire type char `C`, the in-game editor's "Copy Program" action) — a blueprint (`B`, components/frames wrapped around a behavior) decodes to a different table shape `decompile_behavior` doesn't expect, so this CLI can't round-trip one yet. Verified end-to-end against a real fixture (`tests/data/beacon.dcs`): decompile → compile → re-decompile produced byte-identical BSF text.

**Not done yet (for `ast_compiler.py`/`Interpreter` specifically — the decompiler/compiler-to-BSF gap above is now closed, see the `bsf/` bullet):** reusing the real `GetFactionBehaviorAsm` compiler from `data/library.lua` (this harness has its own simplified stand-in for now, `Interpreter`'s `Memory`/mem-slot allocation); reusing the real `InstBeginBlock` for `sequence`/`for_number` block-stack driving itself (both still Python-simulated, though `for_number`'s own per-iteration decision is now delegated to real Lua — see `interpreter.py` above); `or`/nested boolean expressions in `if`; coordinate/id/entity literals in source syntax. `ast_compiler.py` itself is slated for retirement in favor of BSF (see the `bsf/` bullet above) — do not use it as a design reference for any new compiler/decompiler work in this project, even stylistically; it may serve later only as an independent test oracle.

## Design Docs Built on This Data

`instructions_index.md` (workspace root) is an auto-generated reference of every entry in `data.instructions` (`data/instructions.lua`) — visual-editor name, op id, and argument list (in/out/exec, in declaration order, with filter type tags). Regenerate by re-parsing that file if it changes. `behavior_format.md` (workspace root) is the companion reverse-engineered spec for the *wire* format a behavior decodes into/from — register/slot addressing, branch and fall-through resolution, `make_asm`/hidden-literal (`c`) fields, and variable-length-arg instructions — cross-checked line-for-line against `observer.dcs`. It predates `desynced_toolkit.dcs_wire` and was originally written against the retired `dsc_codec.py`'s 0-based Python dict rendering; its description of the *actual wire format* (register addressing, branch resolution, hidden literals, etc.) is still accurate, but where it talks about "dsc_codec.py" or 0-based dict keys as if that's how the data is held in memory, that's the old tool's rendering choice, not the format itself — `dcs_wire.py` now hands back the same information as genuine 1-based Lua tables instead. Together with `instructions_index.md`, these are the missing layer between the wire bytes (transport only, no instruction semantics) and hand-authoring a new behavior; read both before writing or editing any `.dcs` content by hand, or before answering *any* question about register/value semantics, branch/jump encoding, or behavior control flow — not just when actively editing a `.dcs` (see "Non-obvious engine/format gotchas" below for the index of specific traps this file documents).

`behavior_source_format.md` (workspace root) is a layer above `behavior_format.md`, not a replacement for it: a specification for a graph-native textual read/edit representation of behaviors (nodes with explicit branch edges, not tree-structured pseudocode), adopted 2026-07-06 after empirical analysis of 319 real user-created behaviors (see project memory) found tree-structured syntax a poor fit for a large share of real cases — `jump`/`label` computed dispatch alone (20.7% of real behaviors) is indirect goto by construction and cannot be expressed as `if`/`else`/`for`. Read it before implementing or reasoning about any decompiler/compiler work in `desynced_toolkit` — it supersedes `compiler/ast_compiler.py`'s Python-like target as the intended direction, and as of 2026-07-08 is a real, working, bidirectional pipeline (`desynced_toolkit.bsf`, see that package's own bullet above), not just a spec — read its own "Status" section for the full detail, including two real grammar gaps found and closed during implementation.

`combat_squad_spec.md` (workspace root) is a hand-written behavior design spec (squad AI roles: Beacon/Scout/Gunner/Support) built from real instructions in `data/instructions.lua` and components in `data/components.lua` — a consumer of this extract, not part of the extract itself. `observer.dcs` is a companion example behavior clipboard string it references, decodable via `desynced_toolkit.dcs_wire` (or, historically, the now-retired `dsc_codec.py`). `beacon.dcs` is a hand-authored implementation of the spec's §5.1 Beacon role (built using `instructions_index.md` + `behavior_format.md`, round-trip-verified against the codec) — target selection is implemented via reservoir sampling rather than the spec's literal shortlist-array pseudocode (a proven equivalent that avoids Memory-array instructions and, at the time, an unconfirmed `REG_INFINITE` literal — since confirmed, see the "Reserved sentinel numbers" gotcha above, though this design choice hasn't been revisited), and exact-dedup of duplicate reports is intentionally omitted (no native set/contains instruction exists; the spec's own §7 already treats near-tie imprecision as acceptable). Not yet tested in-game. (`observer.dcs`/`beacon.dcs`/`beacon2.dcs` all live under `tests/data/` now — see the note at the end of this section.)

`hex_expansion_math.md` (workspace root) is a hand-worked-out coordinate-math spec for a hexagonal-spiral power-pole expansion behavior (`HexAt(origin, R, T) -> coord` and its inverse `HexIndexOf`), built against the integer-only constraints of the Math instructions. `HexAt` is implemented and validated in-game: `hexat_test.dcs` (`tests/data/`) is a self-contained `.dcs` bundling both a reusable `HexAt(R, T, Origin, d_half) -> Result` sub-behavior (via the top-level `dependencies` mechanism documented in `behavior_format.md`) and a test harness that calls it for every `R=0..5, T=0..6R-1` and `debug_print`s the result; `hexat_test_log.txt` (workspace root — a captured log, not a `.dcs`, so it stayed put) is the captured log from that run. Cross-checked by hand against `hex_expansion_math.md`'s formulas for all 92 logged `(R, T)` cases with zero mismatches. This round of building/loading/fixing/re-exporting against the real game is also what surfaced and corrected several wrong assumptions in `behavior_format.md` (coordinate-literal encoding, `exit`/`restart`/block-scoped `next: false` semantics, the `call`/`dependencies` embedding mechanism, the `jump`/`label` computed-dispatch pair) — read `behavior_format.md` itself for the details rather than re-deriving them. `HexIndexOf` now has a `.dcs` too: `HexIndexOf_test_1.dcs` (`tests/data/`) bundles a `HexIndexOf(Coord, Origin, d_half) -> (R, T)` sub-behavior alongside `HexAt` as a second embedded dependency, plus a harness that round-trips `HexAt`'s output back through `HexIndexOf` for every `R=0..5, T=0..6R-1` and `debug_print`s both pairs for comparison. Originally self-checked (before `desynced-toolkit` existed) with an independent hand-rolled Python re-implementation of the instruction semantics; now much more strongly validated by `tests/test_hex_expansion.py`, which runs both `HexAt` (against closed-form reference math) and `HexIndexOf` (round-tripped through `HexAt`'s own output) through the *real* `data/instructions.lua` via `Interpreter` — 217 `(R, T)` cases up to `R=8`, all correct. **Now also loaded and run in the actual game client** — spot-checked (not exhaustive) log output was correct, but the user found the generated node graph itself poorly organized (too many single-use temp variables, no visual grouping/separation) and plans to hand-rewrite it in the in-game editor for clarity, then export back for a diff against the current `HexIndexOf_test_1.dcs` — see `hex_expansion_math.md`'s "Open items" section for the full note; this is queued future work, not started. Separately, the *design* half of that readability complaint (fewer single-use temps, deliberate grouping, avoiding duplicated cascades) got a real second pass: a user-driven review of the pseudocode caught `d` being computed only to feed `Den` (folded into one precomputed constant `4*K`), a 4-instruction split-then-subtract coordinate pattern collapsible to 2 (subtract the coordinates directly, then split once), a dead `ry` reassignment in the cube-rounding correction that was never read again, and reframed the 6-region cascade as a `for_number` search loop with `jump`/`label` computed dispatch (reusing `HexAt`'s own proven idiom, previously dismissed for this spot for a reason that turned out to only rule out one particular use of it) instead of 6 unrolled region blocks each duplicating its own range-check. All of this is now compiled (not just hand-derived) and validated end-to-end: the compiler (see `compiler/ast_compiler.py` above) grew loops/`break`/`continue`/`jump`/`label`/nested-arithmetic/`abs`/`max`/coordinate-op support specifically to express it, and `tests/test_hexindexof_compiled.py` runs the compiled result through the real `data/instructions.lua` via `Interpreter` against the same 217 `(R, T)` reference cases (`R=0..8`) `test_hex_expansion.py` uses, all passing. This compiled version is a from-scratch rebuild of the revised design, not a re-export of `HexIndexOf_test_1.dcs` — that `.dcs` still reflects the original pre-review design (own note in `hex_expansion_math.md`'s "Open items"), and reconciling the two (by hand-rewriting in the in-game editor, per the queued follow-up above) is still separate, not-yet-started work.

`blight_magnifier_mining.md` (workspace root) is a two-part design doc: (1) the real component-efficiency math (`get_work_time`, `data/components.lua:7405`) applied to picking building layouts for Blight Magnifiers — overclock modules' additive building-wide boost, the flat +50% oversized-socket bonus, and a corrected finding that per-slot brute-force enumeration (not a shortcut formula) is required once a building has 2+ magnifiers and a Large socket, since the oversocket bonus is per-component while an Overclock Module's boost is shared; (2) a full `MinerDrone` behavior written directly in `behavior_source_format.md`'s graph grammar (reservoir-sampled node selection, `Loop Signal`/`FRAMEREG_SIGNAL`-based oversubscription avoidance, corrected to rely on the native store-register auto-deliver/auto-resume cycle rather than re-driving the miner every tick) — the first real test of *authoring* a behavior directly in that format from a natural-language design discussion, not just decompiling one. Confirms several native mechanics not previously documented: `drone_range` only gates the native automatic port-dispatch system, not a Program-controlled unit (`c_miner`'s own search uses `visibility_range` instead); the store register auto-resumes a drone's previous mining target after an auto-delivery trip, with no Program involvement, even with no Program installed at all; a resource node mined to exactly 0 can never be revived (`AddResourceHarvestItemAmount`'s `num > 0` guard); and `set_reg_remotely` cannot target a neutral resource node (`GetAdjacentFactionEntityOrSelf` requires same-faction-or-self). Not yet compiled/round-tripped through real Lua or tested in-game — see the doc's own "Known gaps" section.

**Major updates 2026-07-11/12, driven by real in-game testing rather than source-reading alone (see the doc's own later sections for full detail):** (1) `MinerDrone`/`MagnifierSignal` have since actually been loaded, run, and hand-edited in the real client (`library/miner_drone.dcs`, `library/magnifier_signal.dcs`), and Mining Leader/Follower/the Hauler were reviewed too (`library/mining_leader.dcs`, `mining_follower.dcs`, `hauler.dcs`) — Mining Leader gained a `Check Avoidance` subroutine (a building broadcasting `v_alert[num=range]` on its `@signal` keeps roaming squads from depleting managed nodes) after a real regression (a validated random-walk destination that was never actually applied to `@goto`) was found and fixed. (2) **A load-bearing earlier claim was reversed**: Overclock modules do speed up mining drones after all — `SetStateStartWork` applies the `eff_boost`/Overclock calculation *natively*, even when the calling component's Lua never references `get_work_time` (true of `c_miner`/`c_adv_miner` *and*, it turns out, of `c_blight_magnifier`/`c_blight_extractor` too — `get_work_time` is Lua-side only for a bespoke tooltip's *display*, not for applying the effect). Confirmed via two independent real in-game stopwatch tests. `faction.component_boost` (the "Re-Simulator global bonus, usually 0") is the real, sometimes-nonzero Global Overclocking Boost component, not always 0. (3) A **real in-game-built blueprint** (`library/magnifier_lattice.dcs`) settled the building-layout-density question empirically rather than by further speculation: a fully-tiled node gets regen from 6 big (3M1L) + 2 small (`f_building2x1c`, 2M not 1M) buildings, needing an oversubscription cap of 6-9 depending on resource, not the originally-guessed flat 2. (4) Two more real engine bugs found and fixed/identified: `is_same_grid` fails when its second argument is a non-owned/world-faction entity (e.g. a resource node) passed directly rather than via `get_location` first; `for_signal_match` can accidentally match a resource node through its signal value's embedded `entity` field, fixed the same way `MinerDrone` already rejects dropped items (`match(..., Filter=v_mineable)` or the combined `v_resource`). (5) Neither Obsidian nor Laterite is mineable by `c_miner`/`c_adv_miner` at all (absent from both items' `mining_recipe`) — only `c_extractor` (a real placeable Medium-socket component) or whole slot-less Human/Alien Miner Mech units work, no flying option exists, and the dense 6-big/2-small lattice built for the other four resources turns out to badly oversupply these two (extractors are much slower) — the original sparse single-building pattern from earlier in the doc is the better fit instead.

**On `.dcs` file locations:** these clipboard-string files were originally treated as temporary, uncommitted pastes from the game (view-only scratch, never checked in) — `observer.dcs`, `beacon.dcs`, `beacon2.dcs`, `formation-hold.dcs`, `hexat_test.dcs`, `HexIndexOf_test_1.dcs` now live under `tests/data/` and **are checked in**, specifically because `tests/test_dcs_wire.py`/`test_hex_expansion.py` depend on them for validation — a fresh checkout needs them present for the test suite to run at all, which an uncommitted scratch file can't guarantee. Any *other* `.dcs` file that's genuinely just a temporary paste-to-view (not referenced by any test) can stay uncommitted at the workspace root as before.

**`library/` (workspace root, added 2026-07-11)** holds the *reference* copies of real, actively-used-in-game behaviors/blueprints, re-exported from the in-game library after live editing sessions — distinct from `tests/data/`'s fixtures (which back automated tests) and from workspace-root scratch pastes (which are throwaway). Currently: `miner_drone.dcs`, `magnifier_signal.dcs`, `mining_leader.dcs`, `mining_follower.dcs`, `hauler.dcs` (Fendersons Transport), and `magnifier_lattice.dcs` (a real in-game-built blueprint, wire type `B` not `C` — decodes to a `{multi, dependencies}` shape rather than the `bsf` package's expected behavior shape, so it needs a direct `LupaEngine.decode_dcs` call rather than the `bsf` CLI). These are living documents the user re-exports here after further in-game edits, not one-time snapshots — check `git log` on a given file for its edit history rather than assuming the checked-in version is final.

## Corpus & Analysis Tooling (`scripts/`, `corpus/`)

Built 2026-07-06 to ground `behavior_source_format.md`'s design decisions in real usage rather than hand-picked examples, and now also the closest thing this project has to a working decompiler. Not covered by `uv run pytest tests/` — these are standalone scripts, exercised so far by direct manual runs and one real live edit (see below), not an automated suite; a gap worth closing before relying on them for more than ad hoc use.

- `scripts/collect_clipboard_corpus.py` — watches this environment's clipboard (works because the dev machine is a VirtualBox guest with the actual Windows gaming host — shared host/guest clipboard, confirmed live 2026-07-06) and harvests any `.dcs`-shaped string that appears, meant to be run by hand while browsing Discord's official Desynced server forum channel doing select-all-copy per thread. Captures paired context (thread text/description) alongside each decoded item, not just the raw string.
- `scripts/collect_steam_forum.py <discussion_url>` — scrapes a Steam Community discussion thread directly (plain server-rendered HTML, paginates via `?ctp=N`, no login/JS needed) for the same kind of shared `.dcs` strings, pairing each with its comment author/text.
- `corpus/` (gitignored — bulk redistribution of other players' shared creative work, kept local-only, not for the repo) — 154 real decoded behaviors/blueprints collected this way as of 2026-07-06, split `discord_behaviors/` (89), `steam_behaviors/` (62), `github_fixtures/` (3, from `ribrdb/desynced-tools`'s test data only — that repo's own conversion *logic* is explicitly not trusted or reused, see the standing directive in project memory). Each source dir has `index.jsonl` (one summary record per item) and a `context/` subdirectory with the surrounding text each item was found in.
- `scripts/analyze_corpus.py` — the statistical analysis that produced `behavior_source_format.md`'s empirical justification: walks every corpus item, generically finds behaviors wherever they occur (top-level, embedded in a blueprint's `c_behavior` component, nested under `subs`/`dependencies`) rather than assuming one fixed container shape, and reports real control-flow fan-in, `jump`/`label` usage, `call` usage, loop usage, and data-variable fan-out across the whole corpus.
- `scripts/render_examples.py <dcs_file> <behavior_path> <out_prefix>` — CLI wrapper (as of 2026-07-08) over the real `desynced_toolkit.bsf` package (see that package's own bullet above): decompiles a real behavior and writes `<out_prefix>.bsf.txt` (real BSF surface syntax) and `<out_prefix>.mmd` (Mermaid). Previously the decompiler prototype itself (DOT/Mermaid/an approximate listing, superseded by the real package — that history, including the real bugs found via a 2026-07-06/07 live in-game edit, is preserved in `behavior_source_format.md`'s "Status" section rather than duplicated here).

## Modding Research

There is no `mods/` directory in this repo (it was never committed, and has since been removed from disk — the two Steam Workshop mod zips it held, `AdvancedCodingTools.zip` and `BehaviorVariablesDebug.zip`, were moved to storage outside this repo; the rest, including the `ClaudeBridgeTest/` diagnostic mod, wasn't kept). The findings from reading those mods' source are still real and still relevant, and live in project memory rather than in-repo files: they patch the live behavior editor's own UI class (`UI.GetRegisteredLayoutClass("Program")`/`UI.GetRegisteredLayoutString("Program")`), which is the confirmed, working answer to "how would a mod hook into the live editor" — see project memory for the full detail (`Program.Refresh`/`construct` wrapping, `self.code` direct mutation, etc.) and for the retracted `GetMainWindow()` dead end. If this research resumes, get the mod zips back from wherever they were moved to rather than re-downloading from Workshop.

## Non-obvious engine/format gotchas — check before answering

These are confirmed-the-hard-way facts — each one was either misread from Lua source alone and corrected against real in-game behavior, or is a sharp edge easy to assume the "obvious" wrong thing about. `behavior_format.md` has the full detail, worked examples, and confirmation method for each; this index exists so the fact of a gotcha's existence surfaces even when the current question doesn't look like ".dcs editing" (e.g. a plain conceptual question about how register values work). Check the cited section before asserting any of these from memory, from the Lua source alone, or from general assumption:

- **Register/value composite semantics** — `coord`/`entity`/`id` are mutually exclusive on a value (setting one clears the other two); `num` is separate and coexists with any of them. `add`/`sub` combine them non-uniformly by operand shape (coordinate⊕coordinate adds both parts including `num`; coordinate⊕bare-number broadcasts the bare number onto both coordinate components and leaves the coordinate side's `num` untouched; entity/item⊕bare-number passes the entity through and adds `num` normally). `behavior_format.md` § "Composite values and the `num` field" (~line 137).
- **Coordinate literal shape** — must be `{"coord": {"x": x, "y": y}}` (keyed object), never a `[x, y]` array — the array form round-trips fine through the codec but loads in-game as silently corrupted. `behavior_format.md` ~line 124.
- **`exec`/`next` jump targets are raw 1-based Lua positions**, not the 0-based dict keys used elsewhere in decoded output — subtract 1 to get the dict key (add 1 when hand-authoring). `behavior_format.md` § "Off-by-one" (~line 248).
- **`exit` genuinely halts the behavior** — nothing runs again until something else explicitly restarts it, despite `state.counter = 1; return true` reading from source alone like "yield and resume next tick." `restart` unconditionally jumps to Program Start (unwinding the *entire* subroutine call stack in one step if called from inside a `call`ed sub-behavior, not just returning one level) but does **not** yield — it can still spin forever under `unlock` with no other yield point. `behavior_format.md` § "Stopping a behavior" (~line 321).
- **`next: false` inside a block (loop/`sequence`) pops to the *enclosing* block's own next-step logic**, not Program Start — only true top-level fall-off does that. `behavior_format.md` § "Block-type instructions" (~line 372).
- **`check_number`'s "Equal" outcome isn't a numbered arg slot** — it's carried by the instruction's ordinary top-level `next`/fallthrough. To test equality, wire both `If Larger` and `If Smaller` to the same "not equal" target and let fallthrough be "equal". `behavior_format.md` ~line 429.
- **An omitted `exec` arg never consults the top-level `next`** — `If Larger`, `If Smaller`, and `next` (Equal) are three independent slots, each with its own fallthrough-to-next-instruction default; setting `next` does not also cover an omitted `If Smaller`/`If Larger`. `behavior_format.md` ~line 445.
- **`for_number`'s omitted `Step` auto-detects direction from `From`/`To`**, including flipping sign at runtime for a bound expression that can invert (e.g. `6*R - 1` at `R=0`) — this silently reverses direction and runs at least once, rather than running zero times the way a fixed-step loop would. `behavior_format.md` ~line 414.
- **`call`'s `sub` field** is a string (saved-library id), *or* a small positive int (1-based index into the top-level `dependencies` array for an embedded sub-behavior), *or* `-1` (recursive self-call sentinel, distinct from the legacy runtime's `0`) — never assume it's always a library id. `behavior_format.md` ~line 506.
- **`keepvars`/`keeparrays` only ever apply to the outermost behavior assigned to the component, never to a `call`ed sub-behavior** — a subroutine's own local variables/memory arrays are always freshly allocated on every single `call` and unconditionally discarded on return, regardless of what its own `keepvars`/`keeparrays` say; even `restart`/`exit` fired from deep inside a nested call unwind the *entire* call stack first and consult only the outer program's own flags. `behavior_format.md` § "Top-level envelope" (`keepvars`/`keeparrays` bullets, ~line 53).
- **An omitted `next`/exec-arg field is a real, explicit wire, not "no decision was made"** — the in-game editor's own compiler only omits it when there genuinely is a connection to the instruction placed immediately after (redundant with position, so it skips writing an int); a pin the user actually left unconnected always gets explicit `false` written, unconditionally, regardless of what happens to physically follow. So "omitted" and "false" are never interchangeable-by-accident: they're two different authorial states, and only `false` means a genuine dead end. `behavior_format.md` § "Branch and fall-through resolution"; `desynced_toolkit.bsf.argcache.resolve_branch`'s docstring has the full 3-way case analysis. (BSF's own text format deliberately still renders the omitted case as plain fallthrough-by-position and recomputes it fresh on every reorder/recompile — a considered choice for text editability, not a claim about what the original wire was; see `behavior_source_format.md` § "Node identity vs. wire position".)
- **...except that "omission is a deliberate wire" only holds for a pin that existed in the instruction's schema when the behavior was compiled** — instructions gain new exec pins across game updates (`domove`/`dodrop`'s "Path Blocked" pin was added after some real saved behaviors were authored), and old data simply never had that arg slot to omit deliberately in the first place. Confirmed in-game (2026-07-09): the current editor, opening an old behavior missing this pin, shows it wired to the same destination as plain fallthrough — it cannot distinguish "deliberately left unconnected" from "didn't exist yet," and doesn't try to. The compiled *effect* is identical either way, but don't narrate an old behavior's own omitted pin as if it reflects a conscious authorial choice — it might just be schema age. `behavior_format.md` § "Branch and fall-through resolution" (the "Real exception" callout, right after the omission rule).
- **A duplicate declared arg/pin name (e.g. `for_entities_in_range`'s three args all literally named "Filter") is disambiguated by occurrence count, not raw wire position** — `desynced_toolkit.bsf.argcache.arg_pin_names` renders them `Filter`, `Filter2`, `Filter3` in the order they're declared, regardless of which numbered wire position they actually sit at. An earlier version suffixed by wire position instead, which for `for_entities_in_range` (Range at position 1, so the three Filters sit at positions 2/3/4) rendered the *second* declared Filter as `Filter3` — reading exactly like "the third Filter" to a human, when it's the second. This caused a real, wrong "the filter chain is broken" conclusion about a correct user behavior (fixed 2026-07-10; regression test `test_duplicate_pin_names_disambiguated_by_occurrence_not_position`). When reading BSF text with a numbered pin suffix, the number is "which occurrence of this name," never "which wire position."
- **A jump/label id literal's `num` suffix is a real, load-bearing part of the target, not incidental** — a real user behavior (Mining Leader V3.2) reuses one label icon (`v_broken`) with three different `num` values (bare, `[num=1]`, `[num=10]`) as three genuinely distinct jump destinations, working around there being a limited fixed set of visual-editor label icons to choose from. `desynced_toolkit.bsf.render_text._literal_key` used to key an `IdLit` purely on `.id`, silently conflating same-id-different-`num` labels into one dict entry so every jump to any of them resolved to whichever was inserted last (fixed 2026-07-09; regression test `test_jump_label_annotation_distinguishes_by_num`). When reading or hand-authoring a `jump`/`label` pair, always treat `(id, num)` together as the target identity, never `id` alone.
- **`REG_INFINITE`/`REG_NOT` are `-2147483648`/`-2147483647` (INT32-scale), not Lua's own 64-bit `math.maxinteger`/`math.mininteger`** — confirmed 2026-07-11 via a real in-game test (a behavior copying empty/`0`/`1`/`-2147483647`/`-2147483648` into a Result parameter, read back from the register UI's own display, which shows `"∞"`/`"≠"` only for those two exact values) cross-checked against `ui/RegisterSelection.lua`'s display/clamp/tooltip code three independent ways. Notably `REG_NOT` is the *smaller-magnitude* sentinel, not the larger one — an "opposite extremes" guess gets the ordering backwards. **A leftover input pin left unconfigured in the editor displays blank there, but that blankness doesn't survive into a runtime value** — an omitted `Value` argument resolves through `Get()`'s own empty-register fallback to a real `num=0`, so the register it gets copied into displays `"0"`, identical to an explicit `0` (confirmed by running all five steps and reading `Result` back afterward — it showed `"0"`/`"0"`/`"1"`/`"≠"`/`"∞"`, not a distinct blank for the first case). `behavior_format.md` § "Reserved sentinel numbers" (~line 261); `engine_stub.lua`'s `REG_INFINITE`/`REG_NOT` corrected to match.

`main.zip` (in the external game-data directory, ~290MB) is a full pristine zip of this same asset tree (matches `def.json` + all package directories) — likely the raw download this extract was populated from. `desynced_toolkit.assets.open_asset_source()` can load directly from it (see "What This Is" and the `desynced-toolkit` section).
